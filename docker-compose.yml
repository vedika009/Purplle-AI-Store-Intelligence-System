version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_PATH=store_intelligence.db
    volumes:
      - ./:/app
    command: uvicorn src.api.app:app --host 0.0.0.0 --port 8000 --reload
